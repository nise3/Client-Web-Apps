import {Person} from '@mui/icons-material';
import React from 'react';

const IconPermissionSubGroup = () => {
  return (
    <>
      <Person />
    </>
  );
};

export default React.memo(IconPermissionSubGroup);
