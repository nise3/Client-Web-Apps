import {BusinessCenter} from '@mui/icons-material';
import React from 'react';

const IconPermission = () => {
  return (
    <>
      <BusinessCenter />
    </>
  );
};

export default React.memo(IconPermission);
