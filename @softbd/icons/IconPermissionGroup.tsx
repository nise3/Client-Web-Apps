import {BusinessCenter} from '@mui/icons-material';
import React from 'react';

const IconPermissionGroup = () => {
  return (
    <>
      <BusinessCenter />
    </>
  );
};

export default React.memo(IconPermissionGroup);
