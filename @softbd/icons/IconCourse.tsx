import {BusinessCenter} from '@mui/icons-material';
import React from 'react';

const IconCourse = () => {
  return (
    <>
      <BusinessCenter />
    </>
  );
};

export default React.memo(IconCourse);
