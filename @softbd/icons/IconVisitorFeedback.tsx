import {BusinessCenter} from '@mui/icons-material';
import React from 'react';

const IconVisitorFeedback = () => {
  return (
    <>
      <BusinessCenter />
    </>
  );
};

export default React.memo(IconVisitorFeedback);
