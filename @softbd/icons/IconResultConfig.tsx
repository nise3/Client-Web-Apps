import React from 'react';
import EditIcon from '@mui/icons-material/Edit';

const IconResultConfig = () => {
  return (
    <>
      <EditIcon />
    </>
  );
};

export default React.memo(IconResultConfig);
