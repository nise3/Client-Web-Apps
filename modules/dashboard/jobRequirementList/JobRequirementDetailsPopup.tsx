import React from 'react';
interface props {
  itemId: number;
  onClose: () => void;
  openEditModal: (id: number) => void;
}
const JobRequirementDetailsPopup = ({
  itemId,
  onClose,
  openEditModal,
}: props) => {
  return <div></div>;
};

export default JobRequirementDetailsPopup;
