import {createStyles, makeStyles} from '@mui/styles';

const useStyles = makeStyles(() =>
  createStyles({
    root: {},
    PaperBox: {
      padding: 20,
      margin: '20px auto',
    },
  }),
);

export default useStyles;
